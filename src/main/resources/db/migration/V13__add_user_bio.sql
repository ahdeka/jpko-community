-- ============================================================
-- V13: 유저 자기소개(bio) 컬럼 추가
--   공개 프로필에 노출되는 짧은 소개글
--   nullable - 미입력 유저가 대부분일 것으로 예상
-- ============================================================
ALTER TABLE users ADD COLUMN bio VARCHAR(200);