spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST}:5432/jpko_community
    username: ${DB_USER}
    password: ${DB_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: false

jwt:
  secret: ${JWT_SECRET}